# --- General ---
ENVIRONMENT=local
DEBUG=true
PROJECT_NAME=EnterpriseGPT
API_V1_PREFIX=/api/v1
ALLOWED_ORIGINS=http://localhost:3000

# --- PostgreSQL ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=enterprisegpt
POSTGRES_PASSWORD=enterprisegpt
POSTGRES_DB=enterprisegpt

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# --- MinIO ---
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=enterprisegpt-documents
MINIO_SECURE=false

# --- Logging ---
LOG_LEVEL=INFO

# --- Auth: JWT ---
JWT_SECRET_KEY=change-me-to-a-long-random-string-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=14

# --- Auth: Google OAuth ---
# Create at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# --- Google AI: Embeddings & Generation ---
# Create at https://aistudio.google.com/apikey — used for embeddings, query rewriting,
# and (in a later phase) the RAG generation model. Leave blank to run without
# semantic search / query rewriting (BM25 keyword search still works).
GOOGLE_API_KEY=
EMBEDDING_MODEL=models/text-embedding-004
EMBEDDING_DIMENSIONS=768
QUERY_REWRITE_MODEL=models/gemini-1.5-flash
